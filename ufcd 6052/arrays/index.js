
// Criando um array vazio de 20 posições
let idades = new Array(20);

// Preenchendo o array com idades (por exemplo, de 1 a 20)
for (let i = 0; i < idades.length; i++) {
    idades[i] = i + 1;  // Idades de 1 a 20
}

// Exibindo o array de idades
console.log(idades[10]);